apiVersion: v1
kind: Service
metadata:
  name: rag-pipeline-service
  labels:
    app: rag-pipeline-service
spec:
  type: ClusterIP
  ports:
    - port: 8013
      targetPort: 8013
      protocol: TCP
      name: http
  selector:
    app: rag-pipeline-service
