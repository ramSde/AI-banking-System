apiVersion: v1
kind: ConfigMap
metadata:
  name: document-ingestion-config
data:
  database.url: "jdbc:postgresql://postgres-service:5432/banking_documents?autoReconnect=true&useSSL=true&characterEncoding=utf8"
  redis.host: "redis-service"
  redis.port: "6379"
  kafka.bootstrap-servers: "kafka-service:9092"
  minio.endpoint: "http://minio-service:9000"
  chroma.host: "chroma-service"
  chroma.port: "8000"
