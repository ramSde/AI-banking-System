apiVersion: v1
kind: Secret
metadata:
  name: otp-service-secrets
  namespace: banking-platform
  labels:
    app: otp-service
type: Opaque
stringData:
  db.username: "admin"
  db.password: "CHANGE_ME_IN_PRODUCTION"
  redis.password: "CHANGE_ME_IN_PRODUCTION"
---
# Note: In production, use Sealed Secrets or external secret management (Vault)
# This is a template - actual secrets should never be committed to Git
